- name: Roboshop Project - Catalogue
  hosts: all
  become: true
  vars:
    component: catalogue
    schema: true
  tasks:
    - name: NodeJs Setup
      ansible.builtin.import_tasks: nodejs.yml



#component=catalogue
#schema_type=mongodb
#source common.sh

#nodejs